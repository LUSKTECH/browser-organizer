// Chrome tab-group colors — the single source shared by the executor (applies
// them) and the panel (offers them), so they can't drift out of sync.
export const TAB_GROUP_COLORS = ['grey', 'blue', 'red', 'yellow', 'green', 'pink', 'purple', 'cyan', 'orange'];
