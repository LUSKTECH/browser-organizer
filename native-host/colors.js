// Chrome tab-group colors — shared by the parser (validates/coerces the model's
// choice) and the prompt builder (tells the model the allowed set).
export const TAB_GROUP_COLORS = ['grey', 'blue', 'red', 'yellow', 'green', 'pink', 'purple', 'cyan', 'orange'];
